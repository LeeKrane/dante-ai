{
  "name": "dante-ai",
  "version": "1.1.0",
  "description": "Talk to Claude Code out loud, then ask it to build something. Chrome speech-to-text -> claude -p headless -> Fish Audio TTS, with a reactive orb; spoken requests can spawn a real build with file tools and open the finished artifact. No Anthropic API key - runs on your existing Claude subscription.",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/LeeKrane/dante-ai.git"
  },
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.112.4",
    "ws": "^8.18.0"
  }
}
